// Dependencies
import React, { useEffect } from 'react';
import { Container } from './submission-header.style';

const SubmissionHeader: React.FC = () => {
    useEffect(() => { }, []);

    return <Container>
        <h2>New Submission</h2>
    </Container>;
};

export default SubmissionHeader;

